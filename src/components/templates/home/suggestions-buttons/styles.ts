import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  scrollView: {
    marginTop: 24,
  },
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    paddingBottom: 12,
  },
});
